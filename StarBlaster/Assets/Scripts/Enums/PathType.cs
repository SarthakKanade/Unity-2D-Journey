public enum PathType
{
    Any,
    Frontline,  // Straight down or slight curves (Tanks)
    Backline,   // Edges or top hover (Snipers)
    Flank,      // Curvy side entrances
    Rush        // Direct fast lines (Bombers)
}
